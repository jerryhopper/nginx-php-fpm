{% extends 'html.twig' %}

{% block contents %}
    <h1>
        CALLBACK
    </h1>
    <p>
        data <code>{{ dump(data) }}</code>.
    </p>

    <p>
        user <code>{{ user }}</code>.
    </p>


    <h2 class="mt-4">
        Filesystem
    </h2>

    <p>
        The root path for this project is <code>{{ rootPath }}</code>.
    </p>

{% endblock %}
